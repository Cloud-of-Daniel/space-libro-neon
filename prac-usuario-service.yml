server:
  port: 3031
spring:
  application:
    name: prac-usuario-service

  datasource:
    url: jdbc:mysql://localhost:3306/prac-usuario
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
   
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:3030/eureka
  instance:
    hostname: localhost
